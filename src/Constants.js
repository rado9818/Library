export const appName = '@Library:';

export const accessToken = appName + 'accessToken';

export const refreshToken = appName + 'refreshToken';

export const ROLE = appName + 'role';

export const FIRST_NAME = appName + 'first_name';

export const LAST_NAME = appName + 'last_name';

export const ORGANIZATION = appName + 'organization';

export const registerEndpoint = '/register';

export const url = "http://localhost:8080/";

export const api = "api/v1";

export const loginUrl = url + api + "/login";

export const registerUrl = url + api + registerEndpoint;

export const usersMeEndpoint =  url + "/users/me";

export const requests =  "requests";

export const eventListEndpoint = url + api + "/event/list";

export const eventCreateEndpoint = url + api + "/register/event";

export const USER_ID = appName + "/user";